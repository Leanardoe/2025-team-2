@{
    ViewData["Title"] = "Test Page";
}
@model QuerySearch

<h1>
    This is a page for testing
</h1>
<h3>
    Change this page to test your changes and view data.
</h3>
<p1>

    @{
        // am so sorry for the mess here
        QuerySearch search = new QuerySearch();

        List<string> keywords = new List<string>() {"apple","grape" ,"meat"};

        List<string> test = search.KeywordSearch(keywords);

        List<string> r = search.KeywordSearch(keywords);

        string W = "";
        String M = "";
        foreach(var word in r)

        {
            W += " " + word;
        }

        Dictionary<string, int> R2 = search.ScoreList(r);

        foreach (var item in R2)
        {
            Console.WriteLine(
             $"{item.Key}: {item.Value}"
            );
        }



        ViewBag.Fv = W;
      

    }

    <label> @ViewBag.FV.ToString() </label>
   

</p1>
